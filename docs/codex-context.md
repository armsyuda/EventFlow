# Codex 작업 연속성

## 프로젝트 목적

Windows 10/11에서 한 사람이 행사별 준비 업무를 관리하는 Python/PySide6 데스크톱 앱이다. 기본 업무는 날짜 없이 생성하며 사용자가 체크리스트에서 작업기간·마감일을 직접 입력한다. 진행률, 달력, 백업·내보내기를 제공한다.

## 2026-08-12 v0.3.25 자동 일정 완전 제거

- 새 행사와 `기본 항목 가져오기`로 생성되는 모든 업무의 작업 시작일·마감일은 `NULL`로 시작한다. 행사 준비 시작일과 최종 행사일을 업무 날짜에 복사하지 않는다.
- 체크리스트의 빈 날짜는 `미입력`으로 표시한다. 날짜 셀을 눌러 직접 입력할 수 있고 셀 편집기의 `지우기`로 다시 빈 값으로 만들 수 있다.
- 기본 항목 원본 JSON과 DB에서 `anchor`, `start_offset`, `due_offset`을 제거했다. 행사 업무 DB에서도 해당 열과 `schedule_mode`를 제거하고 자동 일정 계산 모듈과 화면의 D-Day 표기를 제거했다.
- 날짜가 하나라도 없는 업무는 달력, 지연 업무와 7일 이내 마감 목록에서 제외한다. 시작일과 마감일이 모두 입력된 업무만 달력에 표시한다.
- DB 스키마는 v7이다. 기존 행사에 이미 저장된 업무 날짜는 사용자 데이터 보호를 위해 보존하고, 자동 일정 관련 열만 제거한다. 이전 전에 `event_checklist.pre-v6.db` 안전 복사본을 만든다.
- 자동검사 75개 통과. 실제 사용자 DB 복사본의 기존 업무 121개를 유지한 채 v7 이전과 외래키 검사를 통과했고, 새 검증 업무 3개가 모두 빈 날짜로 생성됐다. 패키징 실행 파일도 실제 DB 복사본 이전 및 smoke test 종료 코드 0을 확인했다.
- 확인용 실행 파일은 `03_Program/EventFlow_Current/EventFlow.exe`, 설치 파일은 `03_Program/EventFlow_Installer/EventFlow-Setup-0.3.25.exe`다. 설치 파일은 코드 서명되지 않았다.

## 2026-08-12 체크리스트 날짜 달력 표시 수정

- 빈 날짜를 되돌리는 `지우기` 버튼을 좁은 표 셀 안에 날짜 입력칸과 함께 넣어 서로 겹치던 UI 문제를 수정했다.
- 날짜 셀을 눌러도 표 셀 안에는 임시 입력창이나 버튼을 넣지 않는다. 별도 달력 팝업을 열고 하단에 `날짜 비우기`와 `닫기`를 배치한다.
- `날짜 비우기`는 해당 날짜를 `미입력` 상태로 되돌리는 기능이다. 자동검사 75개와 패키징 실행 smoke test를 통과했으며 v0.3.25 확인용 실행 파일과 설치 파일을 다시 생성했다.

## 2026-08-12 업데이트 실패 원인 안내 개선

- 업데이트 확인 스레드가 예외 내용을 버리고 `확인 실패`만 표시하던 구조를 수정해 실제 실패 메시지를 화면까지 전달한다.
- HTTP 404·403·429·서버 오류, 응답 시간 초과, DNS, SSL 인증서, 연결 거부, 잘못된 GitHub 응답을 구분하며 `확인된 원인 / 확인 방법 / 오류 상세` 형식으로 안내한다.
- 다운로드 단계도 Release ZIP 누락, 네트워크 오류, 저장 권한·디스크 공간, SHA-256 불일치를 구분한다.
- 확인 실패 즉시 안내창을 표시하고, 상단 `다시 확인` 버튼의 도움말에 마지막 원인을 유지한다.
- 공개 버전은 확인됐지만 `EventFlow-Windows.zip`이 누락된 경우도 브라우저만 여는 대신 원인과 Release 주소를 안내한다.
- 자동검사 81개가 통과했다. 설치본은 만들지 않고 `03_Program/EventFlow_Current/EventFlow.exe`만 갱신했다.

## 2026-08-12 Windows 설치 파일 생성

- 사용자 요청에 따라 최신 확인용 실행본을 Inno Setup 6.7.3으로 묶어 `03_Program/EventFlow_Installer/EventFlow-Setup-0.3.24.exe`를 생성했다.
- 설치 폴더 선택, 바탕 화면 바로가기 선택, 시작 메뉴 등록, 제거 프로그램을 포함한다. 기본 설치 위치는 `%LOCALAPPDATA%\Programs\EventFlow`이며 사용자 DB와 백업은 설치 폴더 밖의 `%LOCALAPPDATA%\EventCheckList`에 유지한다.
- 컴파일러의 포함 파일 검증과 설치 EXE 생성이 성공했다. 파일 크기는 33,848,560바이트, SHA-256은 `E56DD26AC14160EFABB0944F25F9E3B2364F3481F400305FF2714D52D5F8852E`다.
- 코드 서명 인증서는 적용되지 않아 다른 PC에서 Windows SmartScreen의 게시자 확인 경고가 표시될 수 있다.

## 2026-08-12 PM 담당·직접 일정·정산 선택 개선

- 기본 설정에서 일정 기준과 시작/마감 D-Day 열을 제거했다. 새 업무의 날짜는 행사 기간으로 초기화되며 행사별 체크리스트에서 사용자가 직접 입력한다. 기존 업무 날짜는 행사 날짜를 바꿔도 자동 재계산하지 않는다.
- 행사 정보에 `PM 업체`를 추가했다. 체크리스트는 `마감일 → 담당자(PM) → 업체 → 업체담당자 → 업체담당자 전화번호` 순서이며, PM 담당자는 행사 PM 업체 소속 사람만 선택할 수 있다. 업체담당자도 선택 업체 소속으로 제한되고 전화번호는 자동 표시된다.
- 체크리스트에서 D-Day, 일정, 행사 단가 열을 제거했다. 기존 담당자 값은 업체담당자로 유지하며 새 PM 담당자 값은 별도 저장한다.
- 정산내역은 설정에 등록한 전체 업체를 즉시 선택 목록에 표시하고, 각 대분류 안의 중분류 `기타`를 마지막에 정렬한다.
- DB 스키마는 v6이다. 기존 DB에는 `events.pm_vendor_id`, `event_tasks.pm_assignee_id`만 추가하며 기존 사용자 데이터는 보존한다.
- 자동검사 78개 통과. 실제 사용자 DB 복사본은 행사 1개와 업무 121개를 유지한 채 v6 이전에 성공했고, 소스와 확인용 실행 파일 smoke test 모두 종료 코드 0이었다.
- 설치본은 만들지 않았다. 현재 확인용 파일은 `03_Program/EventFlow_Current/EventFlow.exe`이다.

## 2026-08-12 체크리스트 연락처·분류 정렬 수정

- 재실행 시 `UNIQUE constraint failed: contacts.kind, contacts.name`로 시작하지 못한 원인은 초기화 과정이 폐기된 이름 단독 고유 인덱스를 다시 만들던 순서 오류였다. 연락처는 ID로 구분하고 이름 중복을 허용하며, 조회용 비고유 인덱스만 유지한다.
- 체크리스트 업체·담당자 선택 목록은 행사 참여자로 미리 지정된 연락처뿐 아니라 설정에 등록된 전체 연락처를 즉시 읽는다. 동명이인은 `이름 · 소속 업체` 또는 `이름 · 프리랜서`로 구분한다.
- 체크리스트 열을 `업체 → 담당` 순서로 인접 배치했다.
- 체크리스트와 기본 항목은 마감일이나 마지막 추가 순서 때문에 분류가 흩어지지 않도록, 기존 최초 순서를 유지하면서 대분류→중분류→항목 순으로 모은다. 새 항목도 선택한 분류 안에 배치되어 대분류와 중분류 세로 병합이 한 덩어리로 유지된다.
- 자동검사 75개 통과. 실제 사용자 DB 복사본 시작 검사와 별도 실행 파일 smoke test 종료 코드 0을 확인했다.
- 이 수정은 아래 최신 PM·직접 일정 개선에 포함되었으며 현재 확인용 실행 파일은 `03_Program/EventFlow_Current/EventFlow.exe`다.

## 2026-08-12 03_Program 작업 폴더 정리

- 루트에 누적된 과거 UI 캡처 PNG, 진단·성능·시각검사용 DB, smoke/scale/verify 데이터, 이전 build/dist 결과, Python 캐시와 생성된 spec 파일을 Windows 휴지통으로 이동했다. 실제 사용자 데이터 `%LOCALAPPDATA%\EventCheckList`는 건드리지 않았다.
- 앱 소스와 검사에 필요한 `.venv`, `src`, `tests`, `tools`, 빌드 스크립트·프로젝트 문서·라이선스 문구는 유지했다. 앱에서 실제 사용하는 아이콘·이미지는 `src/event_checklist/resources/assets` 안에 보존했다.
- 현재 확인용 실행본은 찾기 쉽게 `03_Program/EventFlow_Current/EventFlow.exe`로 정리했다.
- 이전 확인용 실행 폴더도 제거되어 현재는 `03_Program/EventFlow_Current`만 사용한다.

## 2026-08-10 구현 결과

- `01_Contents`의 CSV 118개 항목을 기준으로 `카메라다이`, `콘솔다이`를 추가해 120개 기본 항목을 생성했다.
- 반복 오염값 `81`, `#NAME?` 및 기존 행사 데이터는 가져오지 않았다. 원본 파일은 수정하지 않았다.
- `02_Planning`에 8개 단계별 계획·설계 문서를 작성했다.
- `03_Program`에 PySide6 UI, SQLite 저장, 일정 엔진, 백업·복원, Excel/CSV 내보내기와 테스트를 구현했다.
- 행사 생성 시 기본 항목을 스냅샷으로 복사한다. 기본 항목 수정은 기존 행사에 소급되지 않는다.
- 행사 날짜 변경 시 `auto` 일정만 재계산하고 사용자가 날짜를 고친 `manual` 일정은 보존한다.
- SEED는 React 코드를 복사하지 않고 의미 기반 토큰·간격·상태·접근성 원칙을 PySide6 QSS로 재구현했다.
- `새 행사` 창의 인스턴스 변수가 Qt의 핵심 `QObject.event()` 메서드를 가려 Windows 네이티브 충돌이 발생하던 문제를 수정했다. 변수명을 `event_record`로 분리하고 GUI 회귀 테스트를 추가했다.
- 기본 항목 표에 확인 포인트 다음으로 수량, 단위, 업체, 담당을 배치했다. 추가·수정·삭제를 지원하며, 기본 업체·담당도 새 행사 작업에 복사한다.
- 데이터베이스 스키마를 v2로 올렸다. 기존 v1 DB는 실행 시 기본 업체·담당 열을 자동 추가하며, 마이그레이션 전에 안전 복사본을 만든다.

## 검증

- `pytest`: 16개 통과
- 새 행사 창 생성 회귀 테스트: 기본 항목 120개를 불러온 상태에서 네이티브 충돌 없이 생성
- v1 → v2 데이터베이스 자동 이전 및 이전 전 안전 복사본 생성 확인
- 실제 Windows Qt 렌더 캡처: 대시보드, 행사 체크리스트, 달력 확인
- PyInstaller onedir 배포 성공
- 배포 EXE smoke test: 종료 코드 0, SQLite 생성, 기본 항목 120개 확인
- 수정 배포 EXE 재검증: 종료 코드 0, 스키마 v2, 기본 업체·담당 열과 기본 항목 120개 확인
- 배포 위치: `03_Program/dist/EventChecklist/EventChecklist.exe`

## 2026-08-10 v0.2.0 개선

- 실제 사용자 행사 `2026-08-10~2026-10-02`가 기존 규칙에서 4월부터 시작되던 원인을 확인했다. 시작일을 행사 당일로 보고 D-120을 빼던 의미 불일치가 원인이었다.
- `start_date`를 준비 시작일, `end_date`를 최종 행사일로 명확히 표시하고, 120일보다 짧은 준비 기간은 음수 D-Day 오프셋을 비례 압축한다. 사후 양수 일정은 유지한다.
- DB 스키마 v3에서 기존 자동 일정만 새 규칙으로 재계산한다. 수동 일정은 보존하며 `event_checklist.pre-v2.db` 안전 복사본을 먼저 만든다.
- 대시보드의 파란 교차 줄을 제거하고 지연/임박 업무를 의미 기반 옅은 경고색으로 표시한다.
- 모든 스크롤바, 탭, 표 머리의 빈 영역을 밝은 테마로 통일했다.
- 행사·기본 항목·담당/업체 표는 모든 열 너비를 직접 조정·이동할 수 있고 `열 너비 맞춤`을 제공한다.
- 기본 항목은 더블클릭 대화상자 없이 모든 표시 필드를 표에서 바로 편집한다.
- 달력은 개별 업무 기간을 얇은 다중 박스로 표시하고 우측 목록을 카드형 정보 구조로 재구성했다.
- 자동 테스트 20개 통과. 실제 사용자 DB 복사본 v2→v3 이전, 안전 복사본, 준비 시작일 이전 일정 제거, PyInstaller 배포본 기동과 달력 렌더를 확인했다.

## 남은 수동 확인

- 별도 Windows 10/11 PC에서 Python 미설치 상태 실행
- Windows 배율 125%, 150%와 1280×720 화면에서 전체 조작 확인
- 실제 행사 담당자가 120개 기본 D-Day 오프셋을 검토하고 행사 유형에 맞게 조정
- 최종 배포 전 앱 전용 아이콘과 조직명 결정, Qt 라이선스 배포 문구 확인

## 2026-08-10 v0.2.1 달력 가독성 개선

- 달력의 얇은 기간 박스 안에 작은 글씨의 업무명을 표시한다.
- 날짜별 표시 순서는 미완료 우선, 마감일이 가까운 순서, 기본 순서다.
- 셀에는 최대 네 줄만 사용하며 초과 업무는 `+N개 더보기`로 표시한다. 날짜를 선택하면 오른쪽 카드 목록에는 전체 업무가 그대로 나온다.
- 일정이 많은 날에도 날짜 숫자가 가려지지 않도록 업무가 있는 셀의 날짜를 위쪽에 고정해 다시 그린다.
- 자동 테스트 21개 통과 및 실제 행사 DB 복사본으로 달력 라벨·더보기·전체 우측 목록을 렌더 검증했다.

## 주의사항

- 실제 사용자 DB를 초기화하거나 기본 데이터로 다시 덮어쓰지 않는다.
- 백업 복원 전 현재 DB를 반드시 별도 백업한다.
- 기본 일정표는 일반 행사 기준 초안이며 법정 신고·허가 기한을 보증하지 않는다.
# 2026-08-11 이벤트 플로우 0.3.0

- 앱 명칭은 `이벤트 플로우`, 약칭은 `이플`, 실행 파일은 `EventFlow.exe`다. 사용자 데이터 위치 `%LOCALAPPDATA%\EventCheckList`는 유지한다.
- 메인 메뉴는 대시보드/체크리스트/달력/정산내역/설정이며, 앱은 항상 행사 미선택 상태로 시작한다. 선택 전에는 체크리스트·달력·정산내역이 비활성화된다.
- 기본 Windows 제목 표시줄을 이플 전용 상단바로 교체했고 주황색 `이플` ICO/PNG/SVG를 추가했다.
- DB는 v4다. `master_items` 기준 단가/VAT, `events` 예산 VAT 기준, `event_tasks` 행사 단가/VAT/제외 상태·사유, 업체 소속 담당자와 행사 참여 업체/프리랜서 관계를 저장한다.
- v3 이전 시 기존 `cost`를 수량으로 나눈 행사 단가로 옮기며 `event_checklist.pre-v3.db`를 먼저 만든다. 기존 담당자는 소속 없는 프리랜서로 보존한다.
- 체크리스트는 기본항목 가져오기, 일회성 항목, 다중 제외와 사유, 기존 기록 복원을 지원한다. 같은 행사에 같은 기본항목이 중복 생성되지 않는다.
- 달력은 연속 주간 막대, 대분류별 저채도 색상, 업무명, 동적 Lane, `+N개 더보기`, 전체 우측 목록과 숨김 상태 저장을 지원한다.
- 정산은 공급가 기준 행사 단가, 과세/면세, 원 단위 ROUND_HALF_UP, 대분류 소계/전체 합계, VAT 포함·별도 예산 비교를 지원한다. 해당없음과 제외 항목은 합계에서 빠진다.
- 검증: pytest 25개 통과. 실제 사용자 DB 복사본은 v3→v4 전후 행사 1/업무 120/담당·업체 10/기본항목 120으로 동일했다. 1280×720 정산, 1440×900 달력, 125%·150% 시작 화면을 렌더 확인했다.
- 배포 위치: `03_Program/dist/EventFlow/EventFlow.exe`. 패키징 실행 파일은 신규 DB와 사용자 DB 복사본에서 모두 종료 코드 0으로 기동했다.

## 2026-08-11 이벤트 플로우 0.3.3

- 행사 정보 창의 참여 업체·프리랜서 목록을 편집 화면의 남는 높이에 맞춰 크게 확장했다. 새 행사 화면은 1280×720에서도 기본항목 선택이 가능하도록 목록 비율을 별도로 유지한다.
- 체크리스트와 행사 창의 날짜 입력은 화살표를 없애고 입력칸 클릭 시 전용 달력이 바로 열리게 했다.
- 행사 예산·기준 단가·행사 단가·정산 단가는 증감 화살표를 제거하고 천 단위 쉼표와 `원`을 공통 표시한다.
- 정산 대분류 소계는 연한 주황색, 전체 합계는 주황색 바탕과 굵은 흰색 금액으로 강조한다.
- Git 저장소를 `https://github.com/armsyuda/EventFlow.git`의 `origin`으로 연결하고 Windows 빌드·Release 워크플로를 추가했다.
- 앱 시작 시 공개 GitHub Release를 비동기로 확인한다. 현재 버전보다 높은 태그와 `EventFlow-Windows.zip`이 있으면 상단 업데이트 버튼을 활성화하며, GitHub가 제공하는 SHA-256 digest를 검증한 뒤 앱 폴더를 교체하고 재실행한다. 사용자 DB는 설치 폴더 밖에 있어 유지된다.
- 자동 업데이트에 인증 토큰을 내장하지 않는다. 저장소 또는 Release가 비공개이면 앱에는 `업데이트 확인 불가`가 표시되므로 공개 Release 운영이 필요하다.
- GitHub Actions 실행 `31446787924`가 성공했고 공개 전환 준비가 된 `v0.3.3` Release에 `EventFlow-Windows.zip`과 SHA-256 파일이 생성되었다. Release ZIP을 다시 내려받아 digest 일치와 실행 파일 종료 코드 0을 확인했다.
- 검증: pytest 30개 통과. 날짜 입력 클릭 시 달력 표시, 금액 쉼표·화살표 제거, Release 버전 비교와 다운로드 digest 검증을 자동 테스트했다.
- 사용자가 기존 `dist/EventFlow/EventFlow.exe`를 실행 중이어서 해당 폴더는 덮어쓰지 않았다. 검증된 최신 로컬 배포본은 `03_Program/dist-0.3.3-release/EventFlow.exe`다.

## 2026-08-11 이벤트 플로우 0.3.4 고정 설치·인앱 업데이트

- GitHub 저장소가 Public이고 익명 API에서 최신 `v0.3.3` Release 및 `EventFlow-Windows.zip`을 읽을 수 있음을 확인했다.
- 프로그램 고정 위치는 `%LOCALAPPDATA%\Programs\EventFlow\EventFlow.exe`다. 외부 배포 폴더에서 처음 실행하면 고정 폴더로 복사한 뒤 다시 실행하고, 바탕 화면과 시작 메뉴에 `이벤트 플로우` 바로가기를 만든다.
- 이후 인앱 업데이트는 고정 설치 폴더만 교체한다. 사용자 DB·백업·다운로드 파일은 기존 `%LOCALAPPDATA%\EventCheckList`에 분리되어 유지된다.
- 업데이트 후 새 앱이 health 신호를 만들 때까지 이전 설치본을 보존한다. 30초 안에 정상 시작하지 않으면 새 설치본을 제거하고 이전 버전으로 자동 복구·재실행한다.
- GitHub Actions는 릴리스 태그와 앱 내부 버전이 다르면 빌드를 중단한다. 태그 push 시 테스트, Windows 빌드와 Release ZIP 생성은 계속 자동 처리한다.
- 현재 PC에 0.3.4 빌드를 고정 설치하고 바탕 화면·시작 메뉴 바로가기를 만들었다. 고정 설치본 smoke test 종료 코드 0과 테스트 DB 생성을 확인했다.
- 검증: pytest 33개 통과, 공개 Release 실조회 성공, PyInstaller onedir 빌드 성공, 고정 설치본 smoke test 성공.
- 수동 교체 과정의 안전 복사본 `C:\Users\armsy\AppData\Local\Programs\EventFlow.manual-old-20260811095420`은 현재 설치와 별도로 남아 있다. 확인 후 삭제 가능하다.

## 2026-08-11 체크리스트·달력·단위 UI 보완

- 기준 Excel의 `설정!F3:F19`를 다시 확인해 단위 목록을 `개/동/대/식/조/명/세트/매/장/부/회/일/박/팀/m/㎡/기타`로 확정했다. 새 DB와 기존 DB의 빈 단위만 업무 성격에 따라 보정하며 사용자가 입력한 값은 유지한다.
- DB 스키마를 v5로 올렸다. v4→v5 이전 전에 `event_checklist.pre-v4.db`를 만들고, 연결된 기존 행사 업무의 빈 단위도 기본 항목 단위로 보완한다.
- 날짜 입력의 증감 화살표를 없애고 입력칸 전체를 클릭하면 전용 달력이 열리게 했다. 수량은 기본적으로 자연수로 표시하며, 단위는 기준 목록에서 선택하되 직접 입력도 허용한다.
- 대시보드 행사 카드는 별도 선택 버튼 없이 카드 전체 클릭과 Enter/Space로 선택한다.
- 달력 오른쪽 업무 카드에 완료/완료 취소, 내일까지/모레까지/날짜 선택 연기를 추가했다. 완료 업무는 체크리스트와 동기화되고 달력 막대에서는 사라지며 오른쪽 목록의 아래쪽으로 이동한다.
- 체크박스·콤보 목록·달력 업무 카드를 SEED 밝은 테마에 맞춰 둥근 모서리와 상태별 저채도 색상으로 통일했다. 기본 항목의 일정 열은 `일정 기준/시작 D±/마감 D±`와 안내 문구로 의미를 명확히 했다.
- 검증: pytest 39개 통과, PyInstaller 빌드 성공, 고정 설치본 smoke test 종료 코드 0, 테스트 DB 생성, 설치본과 빌드본 해시·폴더 구조 일치 확인. 실제 사용자 DB 복사본도 종료 코드 0으로 v4→v5 이전되었고 `event_checklist.pre-v4.db`가 생성됐다.
- 현재 고정 설치본: `C:\Users\armsy\AppData\Local\Programs\EventFlow\EventFlow.exe`. 사용자 데이터 `%LOCALAPPDATA%\EventCheckList`는 교체하지 않았다.

## 2026-08-11 체크리스트 성능·업데이트 정보·오늘 마감 동작

- 체크리스트 지연 원인은 행마다 날짜 달력을 미리 2개씩 생성한 점, 담당자 목록을 업무마다 반복 조회한 점, 행사 선택 시 숨겨진 체크리스트·달력·정산을 모두 로드한 뒤 메뉴 진입 때 다시 로드한 점이었다.
- 날짜 달력을 최초 클릭 시 지연 생성하고, 담당자는 행사당 한 번 조회해 업체별로 메모리에서 분류한다. 행사 선택 시 대시보드만 즉시 표시하고 다른 화면은 해당 메뉴를 열 때 처음 로드하며, 보이지 않는 화면의 연쇄 새로고침도 제거했다.
- 120개 항목 기준 체크리스트 생성은 신규 검증 DB 203~209ms, 실제 사용자 DB 복사본 209ms였다. 설치본에서 행사 선택 후 체크리스트가 즉시 표시되는 것을 Windows 화면으로 확인했다.
- GitHub 최신 릴리스 정보를 새 버전 여부와 분리해 항상 읽는다. 상단바에 `현재 버전 · 공개 버전 · 공개 날짜`를 표시하고, 새 버전이 없거나 확인 실패 후에도 `다시 확인`할 수 있다.
- 현재 PC의 앱은 로컬 0.3.4, GitHub 공개 릴리스는 0.3.3(2026-08-11)이므로 설치할 더 높은 버전이 없었던 것이 기존 `최신 버전` 표시의 이유다.
- 달력의 미완료 업무 버튼은 체크 표시를 없앤 `완료 처리`로 바꿨다. `내일까지/모레까지/날짜 선택`은 미완료이면서 마감일이 오늘인 업무에만 나타난다. 오늘 마감 카드는 굵은 경고 테두리, `오늘 마감` 배지와 완료·연장 안내로 강조한다.
- 검증: pytest 43개 통과, 실제 사용자 DB 복사본 120개 성능 측정, PyInstaller 빌드 및 고정 설치본 smoke test 종료 코드 0, Windows 화면에서 업데이트 메타데이터·체크리스트·오늘 마감 카드 확인.

## 릴리스 운영 원칙

- 이플의 앱 버전을 올리는 작업은 로컬 수정만으로 끝내지 않는다. 테스트 통과 후 GitHub에 커밋·푸시하고 동일 버전 태그와 공개 Release의 Windows ZIP 생성까지 확인해야 완료로 본다.
- 사용자가 자동 업데이트를 실제 검증할 수 있도록, 새 릴리스를 게시하기 전에는 현재 설치본을 새 버전으로 수동 교체하지 않는다.
- 2026-08-11 현재 변경사항은 `0.3.5`로 게시하며, PC의 `0.3.4` 설치본에서 업데이트 버튼 활성화와 인앱 교체를 검증한다.

## 2026-08-11 v0.3.6 자동 업데이트 재실행 수정

- v0.3.4에서 업데이트 버튼을 누르면 앱 종료 후 다시 실행되지 않는 현상을 재현했다. 생성된 `apply-0.3.5.ps1`과 남은 staging 폴더를 확인했다.
- 원인은 업데이트 PowerShell이 설치 폴더를 현재 작업 폴더로 상속받아, 자기 자신이 사용 중인 설치 폴더를 `Move-Item`으로 교체하지 못한 것이다.
- PowerShell 작업 위치와 Python `Popen.cwd`를 `%LOCALAPPDATA%\EventCheckList\updates`로 고정했다.
- 압축 해제부터 새 앱 health 확인까지 전체 과정을 하나의 복구 범위로 묶었다. 교체 전 실패도 기존 앱을 다시 실행하며 `update-버전.log`에 단계와 오류를 기록한다.
- 격리된 실제 설치 폴더 복사본에서 성공 경로는 `EXTRACT→SWAP→LAUNCH→SUCCESS`, 잘못된 ZIP 실패 경로는 `FAILED→RECOVERY`와 기존 앱 재실행을 확인했다.
- 기존 v0.3.4 업데이터 자체에는 수정 코드를 소급할 수 없어 v0.3.6을 한 번 수동 복구 설치했다. 다음 v0.3.7은 v0.3.6의 인앱 업데이트 버튼으로 설치·재실행을 실제 검증하기 위한 릴리스다.

## 2026-08-11 v0.3.9 자동 업데이트 최종 검증

- v0.3.6에서 v0.3.7로 실제 인앱 업데이트를 시험하면서 업데이트 로그 파일을 외부 진단 도구가 읽는 순간 로그 기록이 실패하고 설치까지 중단되는 추가 조건을 확인했다. 이때는 이전 앱 자동 복구와 재실행이 정상 동작했다.
- v0.3.8부터 로그 기록 실패가 설치·롤백 흐름을 중단하지 않도록 진단 로그를 best-effort 처리했다. 업데이트 작업 폴더 고정과 전체 예외 복구 로직은 그대로 유지한다.
- v0.3.8 앱에서 상단 `업데이트` 버튼으로 v0.3.9를 설치하는 실제 사용자 흐름을 검증했다. 앱 종료 후 새 프로세스로 자동 재실행됐고 로그는 `START→EXTRACT→SWAP→LAUNCH→SUCCESS`로 끝났다.
- health 확인 뒤 `EventFlow.update-old`가 제거됐으며 상단 표시와 좌측 하단 앱 버전이 모두 `0.3.9`로 바뀌었다.
- 사용자 DB는 업데이트 전후 모두 행사 1개, 행사 업무 120개, 기본항목 120개로 동일했다. 사용자 데이터 위치 `%LOCALAPPDATA%\EventCheckList\data`는 프로그램 교체 대상에서 제외된다.
- 자동 업데이트 관련 테스트를 포함한 pytest 43개와 v0.3.8·v0.3.9 Windows 빌드/공개 Release 작업이 모두 통과했다.

## 2026-08-11 v0.3.10 표 행 정렬·분류 경계 개선

- 체크리스트와 설정의 기본항목 표에서 공통 7px 셀 여백 뒤에 최소 높이 42px 입력칸이 배치되어, 42~46px 행의 아래쪽을 최대 7px 침범하던 원인을 확인했다.
- 입력 위젯이 있는 두 표만 셀 위아래 여백을 3px로 줄이고 행 높이를 48px로 통일했다. 날짜·상태·담당·단가·업체·단위 등 모든 입력칸의 아래쪽이 행 경계 안에 들어오는 것을 실제 위젯 좌표로 검증했다.
- 대분류가 바뀌는 행 위에는 3px 주황색 선, 같은 대분류에서 중분류가 바뀌는 행 위에는 2px 중립 회색 선을 표 전체 너비에 표시한다. 체크리스트와 기본항목에 동일한 그룹 구분 규칙을 적용했다.
- 자동검사 44개 통과 및 두 표의 실제 렌더 이미지에서 입력칸 정렬과 분류 경계를 확인했다.

## 2026-08-11 v0.3.11 공통 편집 표·달력·메뉴 디자인 정리

- 체크리스트·기본항목·정산내역의 엑셀형 표를 `configure_editable_table` 공통 컴포넌트로 통합했다. 행 높이, 편집칸 여백, 열 조절, 스크롤, 교차 행 스타일은 한 곳에서 관리하고 분류 경계선은 필요한 표에만 옵션으로 적용한다.
- Qt 콤보박스의 별도 팝업 컨테이너를 투명 처리해 둥근 드롭다운 모서리 밖의 검은 사각 조각을 제거했다.
- 설정 메뉴를 행사 메뉴 묶음에서 분리해 사이드바 하단에 배치했다.
- 달력 이동 버튼과 월 표시를 달력 영역 중앙 상단으로 이동했고, 일정 막대 높이와 글자를 키웠다. 긴 막대는 항목명을 최대 3회 균등 반복해 중간과 끝 구간에서도 읽힌다.
- 정산 소계와 전체 합계는 12pt 굵은 글씨와 높은 행으로 강조했다.
- 검증: GUI 자동 테스트 47개 통과. 1440×900 렌더에서 달력 중앙 정렬, 막대 반복 텍스트, 표 행 범위, 소계 강조, 드롭다운 모서리를 확인했다.

## 2026-08-11 v0.3.12 로컬 확인용 UI·응답성 개선

- 사용자 요청에 따라 이 버전부터는 GitHub에 자동 게시하지 않고, 로컬 Windows 실행 파일로만 검수한다. 사용자가 게시를 요청할 때까지 작업 트리를 유지한다.
- 달력 일정 카드에 시스템·시설·행사·홍보·운영 대분류별 저채도 외곽선을 적용했다. 지연·오늘 마감·완료 배경은 유지한다.
- 행사 등록·수정의 프리랜서 목록은 `이름 · 역할/분야`를 함께 표시한다.
- 앱의 모든 드롭다운을 `AppComboBox` 공통 컴포넌트로 통합했다. 팝업 컨테이너에 투명 배경과 둥근 마스크를 적용해 Windows의 검은 모서리 조각을 차단했다.
- 새 행사 창은 왼쪽 기본정보·참여자와 오른쪽 `필요한 기본 항목` 전용 패널로 분할했다.
- 행사 선택 후 대시보드가 보이는 동안 120개 체크리스트를 사전 생성하고, 달력 등에서 자료가 바뀌어도 보이지 않는 동안 다시 준비한다. 컴포넌트 생성 측정값은 120개 기준 약 0.21초였고, 메뉴 클릭 시에는 재생성하지 않는 것을 검증했다.
- 버튼·체크박스·드롭다운·탭·선택 목록에 공통 손가락 포인터를 적용했다. 텍스트·금액 입력칸은 입력 커서를 유지한다.
- 검증: 자동검사 51개 통과. 새 행사 양쪽 패널, 프리랜서 역할 표시, VAT 드롭다운 마스크, 분류별 일정 카드, 메뉴 진입 시 무재로드, 클릭 포인터를 검사했다. `dist/EventFlow/EventFlow.exe` 패키지 스모크 실행과 0.3.12 렌더를 확인했다.

## 2026-08-11 v0.3.12 우선순위 제거·고속 표 전환

- 사용자 요청에 따라 기본 항목·체크리스트·대시보드·달력·내보내기·생성 도구에서 우선순위 표시, 편집, 정렬 규칙을 모두 제거했다. 기존 DB의 `priority` 열은 이전 사용자 DB를 깨뜨리지 않기 위한 비활성 호환 열로만 남기며 앱에서는 읽거나 쓰지 않는다.
- 체크리스트·기본 항목·정산내역을 `FastEditableTable` 공통 컴포넌트로 통일했다. 모든 행에 콤보·날짜·금액 위젯을 미리 만드는 대신, 누른 셀 하나에만 임시 편집기를 생성하고 입력 완료 즉시 제거한다.
- 정산 입력은 전체 120개 행을 다시 만들지 않고 변경 셀·계산 열·대분류 소계·전체 합계·요약 카드만 갱신한다. 행사 선택 시 체크리스트와 정산내역을 대시보드 뒤에서 미리 준비한다.
- 모든 표의 열 조절, 행 높이, 픽셀 스크롤, 교차 행 색상은 `configure_data_table`에 모았고 편집 표는 이를 확장한다. 담당·업체 표도 같은 기본 표 컴포넌트를 사용한다.
- 120개 업무 기준 오프스크린 측정: 체크리스트 17.1ms, 기본 항목 7.3ms, 정산내역 9.5ms, 끝 스크롤 0.05ms 이하, 정산 수량 입력·합계 갱신 2.25ms. 지속 셀 위젯 수는 세 표 모두 0개다.
- 검증: 자동검사 52개 통과. 우선순위 헤더 부재, 표 준비·스크롤 시간 상한, 지속 편집기 부재, 정산 입력 시 전체 표 재생성 금지를 회귀검사에 추가했다. GitHub에는 게시하지 않았다.

### 공통 드롭다운 검은 배경 수정

- 고속 표의 임시 `AppComboBox` 팝업에서 외부 컨테이너에 지정한 투명 배경이 내부 목록까지 상속되어 Windows에서 목록 전체가 검게 렌더링되는 원인을 확인했다.
- 투명 처리는 `QComboBoxPrivateContainer`에만 제한하고 내부 목록에는 흰색 배경, 중립 글자색, 주황색 선택 상태를 공통 컴포넌트가 직접 적용하도록 수정했다.
- 팝업 렌더 이미지를 픽셀 단위로 검사해 밝은 배경 비율이 60% 이상인지 확인하는 회귀검사를 추가했다. 전체 자동검사 52개가 통과했으며 GitHub에는 게시하지 않았다.
- 정산내역·기본 항목의 직접 입력 가능한 단위 드롭다운은 팝업을 여는 순간 발생하는 `editingFinished`를 실제 입력 완료로 오인해 즉시 닫히는 문제가 있었다. 공통 `AppComboBox`가 팝업 열림 상태를 추적하고, 열려 있는 동안의 포커스 이동은 저장 신호에서 제외하도록 수정했다. 실제 Windows 방식으로 단위 셀 클릭 후 500ms 동안 목록 유지와 선택 후 저장·닫힘을 확인했으며 자동검사 53개가 통과했다.
- 좁은 단위 열에서 팝업도 같은 폭으로 열리고 스크롤바·여백이 공간을 차지해 `세트`가 `...`으로 표시되는 문제를 수정했다. 공통 팝업은 가장 긴 항목을 측정해 최소 112px 너비를 확보하고, 가로 스크롤과 말줄임을 끄며 세로 스크롤바를 5px로 표시한다. 실제 Windows 렌더에서 `세트` 전체 표시를 확인했고 자동검사 54개가 통과했다.
- 체크리스트 고속 표가 `sqlite3.Row`를 화면 상태로 그대로 보관해 상태 선택 후 DB 저장은 시작되지만 화면용 행 갱신에서 `TypeError`가 발생하던 문제를 수정했다. 조회 직후 모든 행을 수정 가능한 `dict`로 변환해 상태·담당·날짜·단가·업체 변경이 같은 경로에서 정상 갱신된다. 실제 Windows 마우스 선택으로 상태 DB 저장, 셀 문구, 완료 체크박스, 편집기 닫힘을 함께 확인했고 자동검사 55개가 통과했다.

### 달력 오늘 표시·이동

- 월간 달력에서 오늘 날짜 숫자를 주황색 원형 배지와 흰색 굵은 글자로 표시한다. 선택 날짜의 연한 셀 배경과 함께 표시될 수 있어 오늘과 현재 선택을 동시에 구분한다.
- 월 이동 버튼 오른쪽에 `오늘로 가기`를 추가했다. 누르면 현재 연·월, 선택 날짜, 오른쪽 일정 목록이 모두 오늘로 이동한다. 중앙 월 표시 정렬은 좌우 균형 공간으로 유지했다.
- 오늘 배지 렌더 픽셀과 다른 달에서 오늘로 복귀하는 동작을 회귀검사에 추가했고 전체 자동검사 56개가 통과했다. 백업 기능은 이번 작업에서 변경하지 않았다.
# 2026-08-11 이벤트 플로우 0.3.13 즉시 저장·되돌리기·백업 체계

- 기존의 변경 즉시 SQLite 저장 방식은 그대로 유지했다. 별도의 저장 버튼을 누르지 않아도 항목·행사·담당자·업체·프리랜서·정산 변경은 즉시 실제 DB에 반영된다.
- 사이드바 설정 바로 위에 `되돌리기/앞으로 되돌리기` 화살표와 `저장` 버튼을 추가했다. 되돌리기는 현재 실행 중인 세션에서 전체 데이터 변경 직전 상태를 최대 50단계 보관하며 Ctrl+Z/Ctrl+Y도 지원한다.
- `저장` 버튼은 미저장 내용을 반영하는 기능이 아니라 현재 전체 DB를 `manual_event_flow_*.db` 복구본으로 만드는 기능이다. 설정의 파일 위치를 직접 선택하는 수동 백업도 그대로 유지했다.
- 변경사항이 있으면 10분마다 전체 DB 자동 백업을 만들고, 정상 종료 시에도 아직 자동 백업되지 않은 변경을 한 번 더 보호한다. `auto_event_flow_*.db`만 최근 10개로 순환 삭제하고 수동 백업과 기존 백업은 삭제하지 않는다.
- 자동·수동 백업은 단일 SQLite 전체 파일이므로 행사뿐 아니라 기본 항목, 업체, 담당자, 프리랜서, 행사별 체크리스트·달력·정산, 앱 설정을 함께 포함한다.
- 검증: pytest 60개 통과, 되돌리기/다시 실행, 이력 제한, 새 변경 후 redo 제거, 자동 백업 10개 회전 및 수동 백업 보존, 실제 사이드바 렌더를 확인했다.
- GitHub에는 올리지 않았다. 로컬 검수용 버전만 0.3.13으로 올렸다.

# 2026-08-11 이벤트 플로우 0.3.14 달력 오늘 버튼 보정

- 달력의 `오늘로 가기` 버튼이 90px 고정 폭과 공통 버튼 좌우 여백 때문에 글자 영역이 부족해 잘리는 원인을 수정했다.
- 현재 글꼴의 실제 텍스트 폭에 좌우 여백을 더해 최소 120px을 확보하고, 월 이동 영역의 좌우 균형용 공간도 같은 폭으로 맞췄다.
- GitHub에는 올리지 않고 로컬 검수용 실행 파일만 갱신한다.

# 2026-08-11 이벤트 플로우 0.3.15 공통 분류 셀 병합

- 공통 `FastEditableTable`에 인접한 대분류·중분류를 계층별로 병합하는 기능을 추가했다. 대분류는 같은 대분류 구간 전체, 중분류는 같은 대분류 안의 동일 중분류 구간만 합친다.
- 기본 항목, 체크리스트, 정산내역이 모두 같은 병합 컴포넌트를 사용한다. 체크리스트의 기존 `분류` 열도 `대분류/중분류` 두 열로 분리해 다른 표와 구조를 통일했다.
- 기본 항목에서 합쳐진 대분류명을 수정하면 해당 대분류 전체, 중분류명을 수정하면 해당 대분류 안의 같은 중분류 전체를 한 번에 변경한다. 한 번의 되돌리기로 복구할 수 있다.
- 정산의 대분류 소계·전체 합계 병합행과 분류 병합이 겹치지 않도록 표를 다시 그릴 때 병합 상태를 공통 초기화한다.
- GitHub에는 올리지 않고 로컬 검수용 실행 파일만 갱신한다.

# 2026-08-11 이벤트 플로우 0.3.16 공통 표 정렬 통일

- 공통 `FastEditableTable`이 항목을 넣는 순간 정렬을 적용하도록 변경했다. 금액 열로 지정되지 않은 모든 셀은 가로·세로 가운데, 금액 열은 오른쪽·세로 가운데 정렬한다.
- 기본 항목은 기준 단가, 체크리스트는 행사 단가, 정산내역은 행사 단가·공급가·VAT·합계만 오른쪽 정렬한다. 업체·담당자·프리랜서 표도 같은 공통 가운데 정렬을 사용한다.
- 날짜·수량·문자·선택형 임시 편집창도 표시 셀과 동일하게 가운데 정렬하고 금액 입력창만 오른쪽 정렬을 유지한다.
- GitHub에는 올리지 않고 로컬 검수용 실행 파일만 갱신한다.

# 2026-08-11 이벤트 플로우 0.3.17 시작 로딩 화면

- 기존에는 `app.py`를 불러오는 순간 메인 창과 전체 페이지·Excel 내보내기 모듈까지 먼저 import해 약 3초 동안 아무 창도 보이지 않았다. 무거운 import를 QApplication과 시작 화면 표시 뒤로 옮겼다.
- 420×214px 이플 전용 시작 화면에 이플 마크, 이벤트 플로우 이름, 무한 진행 표시와 `실행 환경 확인/화면 디자인/행사 데이터/대시보드 구성` 단계 문구를 표시한다.
- 메인 창이 표시되면 시작 화면은 자동으로 닫힌다. 최초 고정 설치 위치 준비 중에도 상태를 표시하고, 검증용 smoke test·스크린샷 모드에서는 시작 화면을 생략한다.
- GitHub에는 올리지 않고 로컬 검수용 실행 파일만 갱신한다.

# 2026-08-11 이벤트 플로우 0.3.18 로딩 화면 중복 방지

- 검수용 `dist` 실행 파일은 고정 설치 폴더로 앱을 복사하고 실제 설치본을 다시 실행하는 부트스트랩 프로세스다. 0.3.17에서는 부트스트랩과 실제 설치본이 각각 시작 화면을 띄워 두 번 나타났다.
- 현재 실행 파일이 부트스트랩인지 먼저 판별하고, 부트스트랩에서는 시작 화면을 띄우지 않는다. 실제로 계속 실행될 고정 설치본에서만 시작 화면을 한 번 표시한다.
- 일반적인 바탕화면·시작 메뉴 바로가기 실행은 기존처럼 시작 화면을 즉시 한 번 표시한다.
- GitHub에는 올리지 않고 로컬 검수용 실행 파일만 갱신한다.

# 2026-08-11 이벤트 플로우 0.3.19 표 체크박스 중앙 정렬

- Qt 표의 기본 체크 표시기는 셀의 TextAlignmentRole을 무시하고 왼쪽 장식 영역에 그려져, 공통 가운데 정렬 후에도 체크리스트 완료·기본 항목 사용 체크가 왼쪽에 남아 있었다.
- 공통 `SpreadsheetItemDelegate`가 셀 배경과 선택 상태를 먼저 그린 뒤 체크 표시기의 실제 크기를 계산해 셀 정중앙에 직접 그리도록 변경했다.
- 분류 경계선을 담당하는 `GroupSeparatorDelegate`도 이 공통 체크 렌더러를 상속하므로 체크리스트와 기본 항목에 동일하게 적용된다.
- GitHub에는 올리지 않고 로컬 검수용 실행 파일만 갱신한다.
# 2026-08-11 이벤트 플로우 0.3.20 체크리스트 순서 열

- 체크리스트의 중복 완료 체크박스를 제거하고 `상태 = 완료`를 유일한 완료 처리 방식으로 유지했다.
- 첫 열을 `순서`로 바꾸고 현재 표시 순서대로 1부터 번호를 표시한다.
- 순서 열은 공통 표의 고정 열 기능으로 52px을 유지하며, `열 너비 맞춤`에서도 남은 열만 화면 폭에 맞춰 조절된다.
- GUI 및 서비스 테스트 65개와 `dist-0.3.20-local` Windows 실행 파일 스모크 검사를 통과했으며, 사용자 요청에 따라 GitHub에는 올리지 않는다.

# 2026-08-11 이벤트 플로우 0.3.21 공통 분류·단위 선택 목록

- 기본 항목 추가·수정 창의 대분류, 중분류, 단위를 공통 `AppComboBox` 기반의 선택/직접입력 방식으로 변경했다.
- 대분류를 고르면 그 대분류에 실제로 등록된 중분류만 우선 표시하며, 목록에 없는 대분류·중분류·단위는 같은 입력창에 새 이름을 입력해 추가할 수 있다.
- 선택 목록은 고정 코드가 아니라 현재 `master_items`와 `event_tasks`에서 매번 구성한다. 새로 저장한 값이 이후 기본 항목 창, 직접 업무 추가, 업무 상세, 체크리스트 대분류 필터, 기본 항목·정산 단위 편집기에 함께 나타난다.
- 공통 선택 목록과 두 Excel형 표의 단위 연동을 회귀검사에 추가했고 전체 자동검사 67개 및 `dist-0.3.21-local` 실행 파일 스모크 검사를 통과했다. GitHub에는 올리지 않는다.
- 선택형 입력이 일반 입력칸처럼 보이는 혼동을 없애기 위해 공통 `AddableChoiceField`를 추가하고 기본 항목 창의 각 입력 옆에 `+ 새 대분류`, `+ 새 중분류`, `+ 새 단위` 버튼을 명시적으로 표시했다.
- 이후 로컬 검수 실행 파일은 버전별 임시 폴더를 만들지 않고 `03_Program/dist/EventFlow/EventFlow.exe` 한 위치만 갱신한다. 빌드 전 EventFlow 실행 여부를 확인하고 실행 중이면 사용자에게 종료를 요청한다.
- `AddableChoiceField`를 편집 가능한 콤보로 만들면 본문 클릭이 목록 열기 대신 텍스트 커서 활성화로 처리되는 문제를 확인했다. 기존 값 선택부는 비편집 드롭다운으로 바꾸고 별도의 `▾` 목록 버튼을 추가했으며, 신규 입력은 `+ 새 …` 버튼으로만 분리했다. 67개 자동검사와 고정 `dist` 실행 파일 검사를 통과했다.

# 2026-08-11 이벤트 플로우 0.3.22 기본 항목 순서·가져오기 안내

- 기본 항목의 `사용` 상태는 행사 생성 시 항목 선택 및 행사별 제외 기능과 중복되고, 체크 표시가 보이지 않을 경우 상태를 오해하게 하므로 UI와 업무 규칙에서 제거했다. DB의 `active` 열은 이전 데이터 호환용으로만 남긴다.
- 기본 항목 첫 열을 체크리스트와 동일한 공통 고정 열 기능의 `순서`로 바꾸고 52px, 1부터 시작하는 숫자, 열 너비 맞춤 제외 규칙을 적용했다.
- 행사 생성과 기본 항목 가져오기는 기존 `active` 값과 관계없이 모든 기본 항목을 대상으로 한다.
- 실제 사용자 DB는 기본 항목 120개가 모두 현재 행사에 이미 포함되어 있어 가져오기 대상 0개가 정상임을 확인했다. 이 경우 오류성 문구 대신 `기본 항목 120개가 모두 포함되어 있음`과 새 항목 추가·제외 후 다시 가져올 수 있다는 안내를 표시한다.
- 전체 자동검사 70개와 고정 `dist/EventFlow/EventFlow.exe` 실행 검사를 통과했으며 GitHub에는 올리지 않는다.

# 2026-08-11 이벤트 플로우 0.3.23 체크리스트 즉시 제외·복원

- 체크리스트의 `선택 항목 제외`에서 제외 사유 입력창을 제거했다. 선택한 행은 한 번의 버튼 클릭으로 즉시 제외하며 `removed_reason`은 빈 값으로 보존한다.
- 제외는 삭제가 아니므로 상태·날짜·금액·메모 등 기존 업무 기록은 그대로 남는다.
- `제외 항목 보기`를 켜면 같은 버튼 문구가 `선택 항목 복원`으로 바뀌고, 제외된 행을 선택해 즉시 원래 체크리스트로 돌릴 수 있다.
- 실제 제외→제외 목록 표시→복원 회귀검사를 포함한 자동검사 71개와 고정 `dist/EventFlow/EventFlow.exe` 실행 검사를 통과했다. GitHub에는 올리지 않는다.

# 2026-08-12 이벤트 플로우 0.3.25 항목 관리·세부내용 표시

- 화면의 `확인 포인트` 명칭을 데이터 원래 의미에 맞는 `세부내용`으로 통일했다. 체크리스트에서 `항목` 바로 오른쪽에 읽기 전용 `세부내용` 열을 추가하고 검색 및 Excel 체크리스트 내보내기에도 같은 값을 포함한다.
- 기본 항목 화면에 `선택 수정` 버튼과 더블클릭 수정을 연결했다. 직접 항목 추가, 기본 항목 가져오기, 상세 수정, 제외·복원에는 실패 원인을 보여주는 오류 안내를 추가했고 제외·복원 전에는 선택 개수를 확인한다.
- 직접 추가 항목도 작업 시작일·마감일을 비운 상태로 생성한다. 단가는 체크리스트 화면과 체크리스트 Excel에는 표시하지 않고 정산내역과 Excel `상세 정산`에서만 사용한다.
- 추가→수정→제외→복원 시 세부내용·수량·단위·단가가 보존되고 정산 계산에 반영되는 회귀검사를 추가했다. 전체 자동검사 77개가 통과했다.
- 기본 항목의 수정·삭제는 이후 생성/가져오기 기준을 바꾸며, 이미 만들어진 행사의 업무 스냅샷은 임의로 변경하거나 삭제하지 않는다.
- 0.3.25부터 GitHub 릴리스 자동화는 프로그램 업데이트에 필요한 `EventFlow-Windows.zip`과 SHA-256 파일만 생성한다. 사용자 요청에 따라 설치 EXE는 더 이상 자동 생성하거나 릴리스에 첨부하지 않는다.

# 2026-08-12 이벤트 플로우 0.3.26 좌측 메뉴 접기·펼치기

- 창 상단 왼쪽에 항상 표시되는 햄버거 버튼을 추가했다. 누르면 212px 좌측 메뉴 전체가 숨겨지고 그 너비를 체크리스트 등 현재 화면이 즉시 사용한다.
- 메뉴가 숨겨진 상태에서도 햄버거 버튼은 제목 표시줄에 남으며, 다시 누르면 좌측 메뉴와 선택 상태가 그대로 복원된다.
- 메뉴 표시·숨김, 체크리스트 영역 확장, 접근성 이름 전환을 회귀검사에 추가했고 전체 자동검사 78개가 통과했다.
- 설치 파일은 만들지 않으며 로컬 확인용 실행 파일만 갱신한다.
